# Lessons

- When a user asks for a large target dataset but we ship an MVP subset first, state the exact seeded count everywhere and never imply the full target is already implemented.
- When the user restates the target as a hard requirement, replace interim partial data with the full requested scope instead of incrementally expanding without hitting the target.
